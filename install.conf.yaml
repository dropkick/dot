- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#   ~/.tmux.conf: tmux.conf
   ~/.zshrc: zshrc

- create:
    - ~/Fonts
    - ~/Video
    - ~/repos
#    - ~/hey/you/guys

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
